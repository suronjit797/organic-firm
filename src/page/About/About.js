import React from 'react';

const About = () => {
        // dynamic title
        document.title = 'Organic Farm || About'
    return (
        <div>
            About
        </div>
    );
};

export default About;