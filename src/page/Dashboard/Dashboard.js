import React from 'react';

const Dashboard = () => {
        // dynamic title
        document.title = 'Organic Farm || Dashboard'
    return (
        <div>
            dashboard
        </div>
    );
};

export default Dashboard;