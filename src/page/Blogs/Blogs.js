import React from 'react';

const Blogs = () => {
        // dynamic title
        document.title = 'Organic Farm || Blogs'
    return (
        <div>
            blogs
        </div>
    );
};

export default Blogs;